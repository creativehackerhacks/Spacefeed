<?xml version="1.0" encoding="utf-8"?>

 <!--This layout is not in use right now.-->
<!--This will be used with "DetailFragment" instead of "DetailActivity"-->

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:id="@+id/container"
    android:layout_height="match_parent"
    tools:context=".fragment.DetailImageFragment"
    android:orientation="vertical">

   <include layout="@layout/toolbar"/>

    <ImageView
        android:id="@+id/frag_image"
        android:contentDescription="Full Image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

</LinearLayout>