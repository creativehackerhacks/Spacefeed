<?xml version="1.0" encoding="utf-8"?>

<!--"DetailActivity" layout-->

<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".activity.DetailActivity">

    <ImageView
        android:id="@+id/cover_image"
        android:contentDescription="@string/full_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:transitionName="demo_image"
        android:src="@color/colorAccent"/>

    <!--This view will overlap the imageView to apply overlaying gradient on the image-->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/over_image_gradient"/>

</RelativeLayout>