<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorOne</item>
        <item name="colorPrimaryDark">@color/colorOne</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="MySimpleToolbarStyle">
        <item name="android:maxHeight">@dimen/abc_action_bar_default_height_material</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
        <item name="titleTextAppearance">@style/Theme.AppCompat.DayNight.DarkActionBar</item>
        <!-- No need for colorPrimary, colorPrimaryDark, colorAccent here
             this should go to the AppTheme -->
    </style>

    <!-- Custom theme for toolbar -->
    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:homeAsUpIndicator">@drawable/ic_backspace_white_24dp</item>
    </style>

    <style name="TransparentToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--<item name="android:background">@null</item>-->
        <!--<item name="android:windowActionBarOverlay">true</item>-->
        <!--&lt;!&ndash; Support library compatibility &ndash;&gt;-->
        <!--<item name="windowActionBarOverlay">true</item>-->
        <item name="android:homeAsUpIndicator">@drawable/ic_backspace_white_24dp</item>
    </style>

    <!-- This theme is to set the background color of the bottom sheet
     + To remove the status bar.
     -->
    <style name="BottomSheetDialogTheme" parent="BaseBottomSheetDialog">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
